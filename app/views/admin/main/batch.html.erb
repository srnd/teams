<h1>Change Current Batch</h1>

<%= render "admin/buttons" %>

<p>
  You really shouldn't need to touch this. At 12 AM every day, this syncs with
  Clear and adds new batches and sets the current one automatically. If you need
  to sync it manually, ask Tyler or TJ.
</p>

<ul>
  <% Batch.all.order("id ASC").each do |b| %>
  <li>
    <% if b.current %>
      <%= link_to content_tag(:b, b.name), admin_set_batch_path(b) %>
    <% else %>
      <%= link_to b.name, admin_set_batch_path(b) %>
    <% end %>
  </li>
  <% end %>
</ul>
