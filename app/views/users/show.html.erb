<h1><%= @user.name %> (<%= @user.username %>)</h1>
<% if @user.current_team %>
	Team: <%= link_to @user.current_team.name, @user.current_team %>
	<br>
	Their team is working on <%= @user.current_team.project || "nothing" %>.
<% else %>
	Team: (none)
<% end %>
